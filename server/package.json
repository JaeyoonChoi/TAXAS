{
  "name": "atax-server",
  "version": "0.1.0",
  "private": true,
  "description": "ATAX 서버 API — Vercel Functions (Edge + Node)",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.24",
    "typescript": "^5.5.4"
  }
}

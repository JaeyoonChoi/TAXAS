{
  "name": "atax-server",
  "version": "0.1.0",
  "private": true,
  "description": "ATAX AI 분석 API — Vercel Edge Functions",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.24",
    "typescript": "^5.5.4"
  }
}
